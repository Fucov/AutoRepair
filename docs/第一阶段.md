第一阶段我建议只做“最小可运行底座 + 可复现故障服务 + 日志与测试”，暂时不接飞书、GitHub、LLM 修复。这样三天内一定能闭环，也为后续 Agent 留出稳定入口。

下面给你一份**第一阶段任务定义文档**和一份可以直接丢给 **豆包 2.0 编程 Agent** 的详细 Prompt。

第一阶段不要急着接飞书、GitHub、LLM 自动修复。先把“可复现故障服务 + 标准日志 + 最小测试 + 项目骨架”打牢。否则后面 Agent 没有稳定输入，会变成反复补坑。

---

# 第一阶段任务定义文档

建议文件名：

```
docs/stage1_task_definition.md
```

内容如下：

```markdown
# Stage 1：最小可运行底座与可复现故障服务

## 1. 阶段目标

本阶段目标是为 FeishuAutoRepair 构建一个小而美、可稳定演示的基础工程。

系统暂不接入飞书、GitHub、LLM 自动修复能力，只完成以下核心基础：

1. 提供一个本地 FastAPI Demo Web 服务。
2. Demo 服务中内置一个可稳定触发的 Python 业务 Bug。
3. 服务异常时能够把完整 Traceback 写入本地日志文件。
4. 提供最小测试集，保证健康路径可测。
5. 提供一个“期望行为测试”，用于后续 Agent 自动修复时验证修复是否成功。
6. 建立简洁清晰的项目结构，为后续日志监听、Agent 修复、GitHub PR、飞书通知预留接口。

本阶段完成后，应能演示：

```text
启动 Demo 服务
→ 访问正常接口成功
→ 访问异常接口触发 500
→ demo_service/logs/app.log 中出现完整 traceback
→ 运行默认测试通过
→ 运行期望行为测试失败，作为后续 Agent 修复目标
```

---

## 2. 项目边界

### 本阶段要做

- Python 3.12 项目基础结构
- FastAPI Demo 服务
- 本地日志写入
- 可复现 traceback
- pytest 测试
- README 与 .env.example
- 最小配置管理
- 最小日志解析工具，可读取最新 traceback

### 本阶段不做

- 不接入飞书 API
- 不接入 GitHub API
- 不调用 Doubao / Ark API
- 不实现自动修复 Agent
- 不创建 PR
- 不做复杂前端
- 不做云部署
- 不引入数据库
- 不引入 Celery、Redis、Docker 等重型依赖

---

## 3. 技术栈

- Python 3.12
- FastAPI
- Uvicorn
- Pydantic / pydantic-settings
- pytest
- httpx
- python-dotenv 可选

---

## 4. 推荐项目结构

```
FeishuAutoRepair/
├── autorepair/
│   ├── __init__.py
│   ├── config.py
│   ├── schemas.py
│   └── log_parser.py
├── demo_service/
│   ├── __init__.py
│   ├── app.py
│   ├── logging_config.py
│   ├── repository.py
│   ├── service.py
│   ├── logs/
│   │   └── .gitkeep
│   └── tests/
│       ├── test_health.py
│       ├── test_profile_success.py
│       └── test_profile_contract.py
├── scripts/
│   ├── run_demo_server.py
│   ├── trigger_demo_bug.py
│   └── print_latest_traceback.py
├── docs/
│   └── stage1_task_definition.md
├── .env.example
├── .gitignore
├── pyproject.toml
└── README.md
```

---

## 5. Demo 服务设计

### 5.1 正常接口

```http
GET /health
```

返回：

```json
{
  "status": "ok"
}
```

---

### 5.2 用户资料接口

```http
GET /users/{user_id}/profile
```

内置 fake user 数据：

```python
{
    "u_1001": {"id": "u_1001", "name": "Alice", "role": "developer"},
    "u_1002": {"id": "u_1002", "name": "Bob", "role": "tester"}
}
```

正常访问：

```http
GET /users/u_1001/profile
```

返回：

```json
{
  "id": "u_1001",
  "name": "Alice",
  "role": "developer"
}
```

---

### 5.3 预埋 Bug

当访问不存在用户时：

```http
GET /users/not-exist/profile
```

当前代码故意不做 None 判断，导致：

```
TypeError: 'NoneType' object is not subscriptable
```

这就是后续 Agent 的修复目标。

期望修复后的行为是：

```json
{
  "detail": "User not found"
}
```

HTTP 状态码应为：

```
404
```

---

## 6. 日志要求

异常必须写入：

```
demo_service/logs/app.log
```

日志中必须包含：

- 时间
- 请求方法
- 请求路径
- 异常类型
- 完整 traceback
- 可用于后续解析的 Python 栈信息

示例：

```
[2026-04-24 15:20:30] ERROR demo_service
Request failed: GET /users/not-exist/profile
Traceback (most recent call last):
  File ".../demo_service/app.py", line xx, in ...
TypeError: 'NoneType' object is not subscriptable
```

---

## 7. 测试要求

默认测试必须通过：

```bash
pytest -q
```

默认测试覆盖：

- `/health` 返回 200
- `/users/u_1001/profile` 返回 200
- 正常用户资料字段正确

另外提供一个后续 Agent 修复用测试：

```bash
pytest -q demo_service/tests/test_profile_contract.py
```

该测试在 Stage 1 阶段应失败，因为 Bug 尚未修复。

失败断言：

```python
def test_missing_user_should_return_404():
    response = client.get("/users/not-exist/profile")
    assert response.status_code == 404
    assert response.json()["detail"] == "User not found"
```

注意：该测试不应该影响默认 `pytest -q` 的通过结果。可以通过 pytest marker 或文件说明实现。

---

## 8. 最小日志解析工具

实现：

```
autorepair/log_parser.py
```

提供函数：

```python
def read_latest_traceback(log_path: str) -> str | None:
    ...
```

作用：

- 从 `demo_service/logs/app.log` 中读取最近一次 traceback。
- 若没有 traceback，返回 None。
- 不需要复杂解析，只要能为下一阶段 Agent 提供稳定输入。

同时提供脚本：

```bash
python scripts/print_latest_traceback.py
```

输出最近一次 traceback。

---

## 9. 启动与演示命令

安装依赖：

```bash
pip install -e ".[dev]"
```

启动服务：

```bash
python scripts/run_demo_server.py
```

触发正常请求：

```bash
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8000/users/u_1001/profile
```

触发 Bug：

```bash
python scripts/trigger_demo_bug.py
```

查看最近 traceback：

```bash
python scripts/print_latest_traceback.py
```

运行默认测试：

```bash
pytest -q
```

运行后续修复目标测试：

```bash
pytest -q demo_service/tests/test_profile_contract.py
```

---

## 10. 验收标准

Stage 1 完成后必须满足：

- 项目能在 Python 3.12 下运行。
- `python scripts/run_demo_server.py` 可以启动服务。
- `/health` 返回 200。
- `/users/u_1001/profile` 返回 200。
- `/users/not-exist/profile` 返回 500，并在日志中写入完整 traceback。
- `python scripts/print_latest_traceback.py` 可以打印最近 traceback。
- `pytest -q` 默认测试通过。
- `pytest -q demo_service/tests/test_profile_contract.py` 当前失败，作为后续 Agent 修复目标。
- 项目结构简洁，无不必要复杂框架。
- `.env` 不提交真实密钥，只提供 `.env.example`。

```
---

# 给豆包 2.0 的详细编程 Prompt

你可以直接复制下面这段给豆包 2.0。

```text
你现在是一个资深 Python 工程师，请你在当前仓库 FeishuAutoRepair 中完成 Stage 1：最小可运行底座与可复现故障服务。

请严格遵守以下要求：项目必须小而美，不要过度设计，不要引入重型依赖，不要实现飞书 API、GitHub API、LLM Agent、自动修复、PR 创建等后续能力。本阶段只做基础工程、Demo 服务、日志、测试和最小日志解析工具。

我的环境是 Python 3.12。

## 一、阶段目标

请实现一个本地 FastAPI Demo Web 服务，服务中内置一个可稳定触发的 Python Bug。当访问异常接口时，服务应返回 500，并将完整 Traceback 写入本地日志文件。项目还需要提供默认通过的 pytest 测试，以及一个当前故意失败的“期望行为测试”，作为后续 Agent 自动修复的目标。

完成后应该能够演示：

启动服务
→ 正常接口返回成功
→ 异常接口触发 500
→ demo_service/logs/app.log 出现完整 traceback
→ 默认测试 pytest -q 通过
→ 修复目标测试 demo_service/tests/test_profile_contract.py 当前失败

## 二、技术栈要求

使用：

- Python 3.12
- FastAPI
- Uvicorn
- Pydantic 或 pydantic-settings
- pytest
- httpx
- python-dotenv 可选

不要使用：

- Celery
- Redis
- Docker
- 数据库
- 前端框架
- 飞书 SDK
- GitHub SDK
- LLM SDK
- LangChain / AutoGen / CrewAI 等 Agent 框架

本阶段代码应保持简单、清晰、可读。

## 三、目标项目结构

请创建或整理为如下结构：

FeishuAutoRepair/
├── autorepair/
│   ├── __init__.py
│   ├── config.py
│   ├── schemas.py
│   └── log_parser.py
├── demo_service/
│   ├── __init__.py
│   ├── app.py
│   ├── logging_config.py
│   ├── repository.py
│   ├── service.py
│   ├── logs/
│   │   └── .gitkeep
│   └── tests/
│       ├── test_health.py
│       ├── test_profile_success.py
│       └── test_profile_contract.py
├── scripts/
│   ├── run_demo_server.py
│   ├── trigger_demo_bug.py
│   └── print_latest_traceback.py
├── docs/
│   └── stage1_task_definition.md
├── .env.example
├── .gitignore
├── pyproject.toml
└── README.md

如果当前仓库已有文件，请先阅读再修改，不要盲目覆盖。若结构已有差异，请尽量兼容并保持简单。

## 四、功能实现细节

### 1. Demo 服务

在 demo_service/app.py 中实现 FastAPI 应用。

必须提供接口：

GET /health

返回：

{
  "status": "ok"
}

必须提供接口：

GET /users/{user_id}/profile

fake user 数据放在 demo_service/repository.py 中，例如：

{
    "u_1001": {"id": "u_1001", "name": "Alice", "role": "developer"},
    "u_1002": {"id": "u_1002", "name": "Bob", "role": "tester"}
}

业务逻辑放在 demo_service/service.py 中。

正常访问：

GET /users/u_1001/profile

返回：

{
  "id": "u_1001",
  "name": "Alice",
  "role": "developer"
}

### 2. 预埋 Bug

请故意在 service.py 中保留一个 Bug：

当 user_id 不存在时，不做 None 判断，直接访问 user["id"]、user["name"]、user["role"]，从而触发：

TypeError: 'NoneType' object is not subscriptable

例如：

def build_user_profile(user_id: str) -> dict:
    user = get_user_by_id(user_id)
    return {
        "id": user["id"],
        "name": user["name"],
        "role": user["role"],
    }

注意：这个 Bug 是本阶段故意保留的，不要修复它。后续 Agent 会自动修复。

### 3. 异常日志

请在 demo_service/logging_config.py 中配置日志。

异常必须写入：

demo_service/logs/app.log

要求：

- 自动创建 logs 目录
- 日志中包含完整 traceback
- 日志中包含请求方法和路径
- 日志格式可读

可以通过 FastAPI middleware 捕获异常并 logger.exception(...)，然后返回 JSONResponse(status_code=500, content={"detail": "Internal Server Error"})。

触发：

GET /users/not-exist/profile

应返回 500，同时 app.log 中出现完整 traceback。

### 4. 最小配置

实现 autorepair/config.py。

要求：

- 使用 pydantic-settings 或简单 os.getenv 均可
- 提供项目根目录、日志路径等配置
- 默认日志路径为 demo_service/logs/app.log
- 不读取真实密钥
- .env.example 只放示例字段，不包含真实值

.env.example 至少包含：

FEISHU_API_BASE_URL=https://open.feishu.cn/open-apis
FEISHU_APP_ID=cli_xxxxxx
FEISHU_APP_SECRET=replace_me
ARK_API_KEY=ark_xxxxxx
ARK_MODEL_REPAIR=ep_xxxxxx
ARK_MODEL_SUMMARY=ep_xxxxxx
GITHUB_TOKEN=github_pat_xxxxxx
GITHUB_OWNER=your_owner
GITHUB_REPO=FeishuAutoRepair
GITHUB_BASE_BRANCH=main
TEST_CMD=pytest -q

### 5. schemas.py

实现 autorepair/schemas.py。

本阶段只需要轻量 schema，为后续 Incident 做准备：

IncidentSource = Literal["local_log", "github_issue", "manual"]

class Incident(BaseModel):
    incident_id: str
    source: IncidentSource
    service: str
    error_type: str | None = None
    traceback: str
    suspected_file: str | None = None
    line_no: int | None = None
    fingerprint: str | None = None
    status: str = "NEW"

不要实现复杂数据库或持久化。

### 6. 日志解析工具

实现 autorepair/log_parser.py。

提供函数：

def read_latest_traceback(log_path: str) -> str | None:
    ...

功能：

- 读取指定日志文件
- 找到最后一次以 “Traceback (most recent call last):” 开始的 traceback
- 返回 traceback 文本
- 若文件不存在或没有 traceback，返回 None

不需要复杂 NLP，只要稳定可用。

实现 scripts/print_latest_traceback.py：

- 调用 read_latest_traceback
- 打印最近 traceback
- 没有 traceback 时打印 “No traceback found.”

### 7. 脚本

实现 scripts/run_demo_server.py：

- 使用 uvicorn 启动 demo_service.app:app
- host=127.0.0.1
- port=8000
- reload=True 可选

实现 scripts/trigger_demo_bug.py：

- 使用 httpx 请求 http://127.0.0.1:8000/users/not-exist/profile
- 打印 status_code 和 response.text
- 这个脚本用于演示触发 Bug

### 8. 测试

请编写 pytest 测试。

demo_service/tests/test_health.py：

- 测试 /health 返回 200
- JSON 为 {"status": "ok"}

demo_service/tests/test_profile_success.py：

- 测试 /users/u_1001/profile 返回 200
- 字段 id/name/role 正确

demo_service/tests/test_profile_contract.py：

这是后续 Agent 自动修复目标测试。本阶段它应该失败。

测试内容：

def test_missing_user_should_return_404():
    response = client.get("/users/not-exist/profile")
    assert response.status_code == 404
    assert response.json()["detail"] == "User not found"

注意：默认执行 pytest -q 时不应该运行 test_profile_contract.py，否则本阶段默认测试会失败。请用 pytest marker 或 pyproject.toml 配置，让默认 pytest -q 排除 contract 测试。

一种可接受方案：

- 给 test_profile_contract.py 标记 @pytest.mark.agent_target
- 在 pyproject.toml 中配置默认 addopts = "-m 'not agent_target'"
- 注册 marker agent_target
- README 中说明运行修复目标测试的命令是：

pytest -q -m agent_target

或者：

pytest -q demo_service/tests/test_profile_contract.py -m agent_target

必须保证：
- pytest -q 通过
- pytest -q -m agent_target 当前失败

### 9. pyproject.toml

请提供 pyproject.toml，包含项目元信息和依赖。

项目名：

feishu-auto-repair

Python 版本：

>=3.12

依赖至少包括：

fastapi
uvicorn
pydantic
pydantic-settings
httpx

dev 依赖至少包括：

pytest

配置 pytest：

- 默认排除 agent_target
- 注册 marker agent_target

### 10. README.md

请写一个简洁 README，包含：

- 项目简介
- Stage 1 当前能力
- 安装命令
- 启动服务命令
- 触发正常请求
- 触发 Bug
- 查看 traceback
- 运行默认测试
- 运行 Agent 修复目标测试
- 当前不支持的内容：飞书、GitHub、LLM 自动修复尚未接入

README 需要让评委或开发者 3 分钟内能跑起来。

### 11. docs/stage1_task_definition.md

请创建 docs/stage1_task_definition.md，内容包括：

- 阶段目标
- 功能范围
- 不做什么
- 项目结构
- Demo 服务说明
- 预埋 Bug 说明
- 日志说明
- 测试说明
- 验收标准

文档不需要很长，但必须清晰。

## 五、验收命令

完成后，请确保以下命令可用：

pip install -e ".[dev]"

python scripts/run_demo_server.py

# 另开终端
python scripts/trigger_demo_bug.py

python scripts/print_latest_traceback.py

pytest -q

pytest -q -m agent_target

预期：

- python scripts/run_demo_server.py 能启动服务
- trigger_demo_bug.py 返回 500
- print_latest_traceback.py 能打印 TypeError traceback
- pytest -q 通过
- pytest -q -m agent_target 当前失败，因为预埋 Bug 尚未修复

## 六、代码质量要求

- 保持简单，不要过度封装
- 每个文件职责清晰
- 不要引入无关依赖
- 不要实现后续阶段功能
- 不要读取真实 .env 密钥
- 不要提交 .env
- 不要修复预埋 Bug
- 不要让默认测试失败
- Windows 路径下也应能运行
- 日志目录不存在时自动创建
- 异常日志必须包含完整 traceback

## 七、输出要求

请你完成代码修改后，最后输出：

1. 创建/修改了哪些文件
2. 如何安装依赖
3. 如何启动服务
4. 如何触发 Bug
5. 如何查看 traceback
6. 如何运行默认测试
7. 如何运行当前故意失败的 Agent 修复目标测试
8. 说明你没有接入飞书、GitHub、LLM，这是因为 Stage 1 只做基础底座
```

---

# 第一阶段完成后的理想效果

录屏时你可以这样展示：

```
1. 打开 README，说明这是 Stage 1 最小底座。
2. 运行 python scripts/run_demo_server.py。
3. 访问 /health，返回 ok。
4. 运行 python scripts/trigger_demo_bug.py，出现 500。
5. 运行 python scripts/print_latest_traceback.py，打印 TypeError。
6. 运行 pytest -q，默认测试通过。
7. 运行 pytest -q -m agent_target，测试失败。
8. 说明：这个失败测试就是后续 AutoRepair Agent 的修复目标。
```

这一阶段做完后，你就有了后续自动修复系统最关键的“靶场”：**可复现 Bug、可解析 Traceback、可验证修复结果**。